fn math_chaos() -> i32 {    
    let a: i32 = 10;
    let b: bool = false;
    
    // HARSH: Adding an i32 and a bool (Type mismatch in binary expression)
    let result: i32 = a + b;
    return result;
}